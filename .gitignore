# Xcode
*.xcodeproj/*
!*.xcodeproj/project.pbxproj
!*.xcodeproj/xcshareddata/
!*.xcworkspace/contents.xcworkspacedata
*.xcworkspace/*
!*.xcworkspace/xcshareddata/
xcuserdata/
DerivedData/
*.hmap
*.ipa
*.dSYM.zip
*.dSYM
.DS_Store
**/GooglePlacesService.swift
**/GolfCourseAPIService.swift
papadot_all_files.txt
Config.xcconfig
*.xcconfig

# xcode-build-server config — embeds machine-local absolute paths and a
# per-machine DerivedData hash, so it can't be shared across contributors
buildServer.json

# Claude Code worktrees (linked worktrees created by Claude agents)
.claude/worktrees/

# Docs now live in the separate endopp.com project; this is a local symlink to it
Documentation
